# Keep request paths for diagnostics, but never persist query strings or referrers.
# Both can contain password-reset, quote, payment, or authentication tokens.
log_format piano_redacted '$remote_addr - $remote_user [$time_local] '
                         '"$request_method $uri $server_protocol" $status $body_bytes_sent '
                         '"$http_user_agent"';
